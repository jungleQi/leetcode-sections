1.适用的数据结构：
matrix , tree, graph

2.实现BFS，需要的容器：
deque:先进先出，先进的是靠近初始点，后进的是远离初始点
set：记录被访问元素，防止某个邻域元素重复遍历，被推入deque，导致死循环

3.核心思想：
3.1 遍历当前出列元素的邻域元素，看是否在visitor中，如果不在就入队列，同时记录到vistior
3.2 出列后，对出列元素进行判断，是否满足预期，如果满足就可以停止返回