1.有序数组合并到其中一个数组，可以考虑从数组尾部开始比较后，将比较结果放到位置，
  这样就不用每次为腾出位置，移动一批的元素
2.链表合并，可以初始化一个头部节点，将比较的结果依次连接在这个节点之后
3.对多个链表的合并，可以将每个链表的每个节点都存入一个列表(这里每个节点没必要是孤立的节点，可以是子链表的头部，
  因为后面排序重新构建之后，每个节点的next都会被重新赋值)，排序，然后将排序之后的节点依次串联成一个链表
